import React from "react";

const Synopsis = (state) =>{
 
  console.log(state);
    return(
      <div style={{color:'antiquewhite'}}>
          Synopsis
      </div>
    )
}

export default Synopsis;